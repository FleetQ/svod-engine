# build outputs
engine/.gradle/
engine/build/
**/build/
.gradle/

# IDE
.idea/
*.iml
.vscode/

# macOS
.DS_Store

# engine internals if a vault is ever checked out here
.svod/
*.svod-tmp

# local tooling / agent indexes (machine-local, never committed)
.serena/
PROJECT_INDEX.json
PROJECT_INDEX.md

# local machine configs (real paths / secret refs)
dist/config.local.*
# personal API keys + POST /secrets values written next to a config that lives here
dist/secrets/
# per-engine state written next to the config (people audit + key activity) — never commit
dist/audit-api.log
dist/user-activity.json
